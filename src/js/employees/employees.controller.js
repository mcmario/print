app
  .controller('employeesController', ['employeesService', '$scope','RootFactory', function (employeesService, $scope,RootFactory) {

      // $scope.peopleList = employeesList;
//       $scope.spinner=true
// ctrl.spinner=false;

      // RootFactory.getUser()
      // RootFactory.


  }])
;